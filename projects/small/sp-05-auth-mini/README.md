# sp-05 — Auth Mini

> JWT authentication added to the Tasks Controllers API (sp-03) with a protected React frontend.

## Spec

See `learning-path/05-small-projects/sp-05-auth-mini.md`.

## How to run

```bash
# Backend
dotnet run --project backend/src/App.Api

# Frontend
cd frontend && npm install && npm run dev
```

## Notes

<!-- Reflections on JWT, password hashing, protected routes, token expiry, etc. -->
